# ── 敏感文件（绝不入库）──────────────────────────────────
.env                    # 真实 API Key
gateway.db              # 运行数据库
*.db
*.sqlite

# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── 运行产物 ────────────────────────────────────────────
nohup.out
*.log
.DS_Store
.env
.claude/

# 数据库备份（含运行数据，仅本地保留）
gateway.db.bak-*
gateway.db-journal*
gateway.db-shm
gateway.db-wal

# ── 前端（Vite）────────────────────────────────────────
frontend/node_modules/
frontend/dist/
